# 2026-09-11

## HaoNav 系统性体检 + 改造方案（方案文档已产出）

产出：`HaoNav-改造方案.md`（项目根目录）。仅出方案，未改代码。

### 实测事实（可复现，后续改造以此为基线）

- `npx vite build` → `dist/assets/index-*.js` **1,617 KB / gzip 359 KB，单 chunk 零分割**；`dist` 中**没有任何 CSS 文件**。
- 样式全部依赖 `cdn.tailwindcss.com` 运行时 JIT（实测 407,279 B / gzip 123,340 B，同步阻塞脚本）。**该 CDN 不可用 = 全站无样式**，属可用性缺陷而非优化项。
- bundle 内含 **1,657 处 lucide 引用 / 4,414 条 SVG path** = 整个图标库。根因：`components/Icon.tsx` 用了 `import * as LucideIcons from 'lucide-react'`，tree-shaking 失效。
- `@google/genai` 与 `jszip` 被打进主 chunk（编辑后台才用）。
- `index.html` 中同一段 site-config IIFE 重复 2 遍；importmap 指向 `aistudiocdn.com`（AI Studio 残留）。
- 根目录非源码资产 ≈ 1.16 MB（10 张 PNG 648 KB + docx 476 KB + 3 SVG 32 KB），只服务 README。

### 关键缺陷（本次体检新发现，非原代码注释所载）

1. **数据链路断裂**：`functions/api/link.ts` 写 KV 键 `app_data`，网页端读写 `haonav_links` → Chrome 扩展存的链接永远不显示。
2. **KV 绑定名两套**：`functions/*` 用 `env.CLOUDNAV_KV`，`api/*`+`edgefunctions/*` 用 `HaoNav_KV`；README 让用户绑 `HaoNav_KV` → 照 README 部署 CF 版本必 500。
3. **`IS_EDGEONE_ENV = hostname.includes('edgeone.app')`**：EdgeOne Pages 实际域名是 `*.edgeone.pages.dev` / `*.edgeone.cool`，判断恒为 false → 在 EdgeOne 上登录永远失败、数据永不上云、静默降级为纯本地。
4. **GET 鉴权可绕过**：`functions/api/storage.ts` 里 `password ? await verifyPassword(...) : true` → 不带 header 即全量公开读取；叠加 `Access-Control-Allow-Origin: *`。
5. **接管漏洞**：`verifyPassword()` 在 KV 无密码时会"把传入的密码存下来并返回 true" → 首个请求者成为站长。
6. **分类密码明文下发**：`Category.password` 随 JSON 返给浏览器，"私密目录"形同虚设。
7. **WebDAV 假成功**：`functions/api/webdav.ts` 只实现"存取 WebDAV 配置"，未实现任何 PROPFIND/PUT/GET 代理 → `check`/`upload` 恒返回 true，`download` 恒返回 null。用户以为有备份，实际没有。
8. **`saveLinks/saveCategories/saveSettings` = `loadAll()` + `saveAll()`**：先读后写，在最终一致的 KV 上存在真实丢写风险。Cloudflare KV 免费版仅 **1,000 写/天**。
9. **死代码 1,067 行**：`CategoryManagerModal`(328 行)、`CategorySortModal`(247 行) 均无入口（`setIsCatManagerOpen(true)`/`setIsCategorySortModalOpen(true)` 在代码中不存在）。
10. **其他**：`id: Date.now().toString()` 同毫秒撞车（`uuid` 被 import 却没用，`buffer` 未使用）；排序靠数组下标无持久化 order 字段；每次 render 执行 `categories.map(filter)` = O(分类×链接)；`vite.config.ts` 的 `define` 会把 `GEMINI_API_KEY` 内联进公开 JS；`vite.config.ts` dev 端口 3000 与 `eop-config.json` 的 5173 不一致；无 strict / ESLint / 测试。

### 用户三点硬要求

视觉尽量不变 · 运行快 · 编辑方便 · 只用 CF/EdgeOne/Vercel 免费套餐。允许推翻语言/框架/依赖/架构。

### 推荐方案（待用户确认后再实施）

- **方案 A**：Svelte 5 + Vite（双入口 `index.html` 前台 / `admin.html` 后台）+ Tailwind v4 构建时 + **Hono 单一 API**（`api/core.ts` 零平台依赖 + ≤10 行 adapter）+ KV 单文档 `nav:v1`（`rev` 乐观并发）+ LexoRank 排序 + 本地字母图标（0 请求）。
- 备选 **方案 B**：保留 React 19 就地加固（去 Tailwind CDN、修 `import *`、动态 import genai/jszip、预分组+防抖、懒渲染、重写后端）→ 首屏约 85–95 KB gzip。
- **删除**：WebDAV、Chrome 扩展生成器（生成代码有 `parentId` 写错等确定性 bug，改用 Bookmarklet）、两个死组件、多余两套后端、`uuid`/`buffer`、`@dnd-kit`、分类密码锁、根目录图片与 docx。

### 平台额度（2026-09 核实）

- Cloudflare Workers Free 100,000 请求/天；KV Free 100,000 读/天 · **1,000 写/天** · 1 GB · 单值 ≤25 MB → 首选，但写入必须合并成 PATCH。
- EdgeOne Pages：流量/请求不限量，Edge Functions 300 万请求/月，KV 1 GB 且**仅 Edge Functions 可调用** → 备选，函数额度更宽。
- Vercel Hobby 最紧且禁商业用途，KV 需外接 Upstash。

### 下一步

等用户确认方案（A / B / 先做止血 5 步）后，再由专家团按 P0→P5 路线图实施。**P0 止血可当天完成且可回滚**：装真 Tailwind、修 `Icon.tsx`、动态 import、修 GET 越权、修平台探测。

---

## 补充（同日）：框架选型与存储选型实测

用户追问「Svelte 5 / Next.js / Vue 3 哪个更优」与「KV / Blob / 数据库怎么选」。为回答，做了实测基准（`/tmp/fwbench` + `/tmp/nextbench`，可复现）。

### 框架运行时基线（生产构建，brotli = 实际传输）

| 框架 | raw | gzip | brotli |
|---|---|---|---|
| Next.js 16.3.4（App Router，单静态路由，默认模板） | 567.6 KB | 174.2 KB | **150.6 KB** |
| React 19.3.0 + ReactDOM | 217.4 KB | 67.3 KB | **57.9 KB** |
| Vue 3.5.42（runtime-only） | 51.9 KB | 20.7 KB | **18.9 KB** |
| Svelte 5.57.0 | 39.1 KB | 14.8 KB | **13.4 KB** |

- Next 测法：`next build` 后解析 `index.html` 实际引用的 7 个 script chunk 汇总。
- 其他测法：只 import 挂载入口（createRoot / createApp / mount），esbuild --bundle --minify，zlib 压缩。

### 单依赖体积（关键发现）

| 写法 | raw | gzip | brotli |
|---|---|---|---|
| `import * as LucideIcons from 'lucide-react'`（全量） | 822.9 KB | 191.4 KB | **146.0 KB** |
| 具名导入约 50 个图标 | 28.9 KB | 9.9 KB | **8.5 KB** |
| `@google/genai` + `jszip` 静态导入 | 454.7 KB | 94.9 KB | **75.2 KB** |

**体积归因**：现有产物 280 KB brotli = lucide 146 KB + genai/jszip 75 KB + React 57.9 KB = 279 KB。**整个包体几乎全是"框架 + 两处错误的 import"，业务代码占比接近 0** —— 改两个 import 即可砍掉 79%。

### 结论修正

- **框架：Vue 3 升为首选，Svelte 5 降为备选。** 原推荐 Svelte 的依据是"Vue 运行时约 34 KB 逼近 45 KB 预算"，实测 Vue 仅 18.9 KB，前提不成立。Vue 加应用代码约 34 KB，预算内有余量，且中文生态与 AI 生成准确率更好。Svelte 5 的额外坑：runes 语法 2024 年底才稳定，AI 容易写成 Svelte 4 的 `export let` / `$:`。
- **Next.js 出局**：其核心价值（SSR/SSG/ISR/RSC/文件路由/Server Actions）本项目一个都不用；上 Cloudflare 需 OpenNext 适配层，上 EdgeOne 需 static export 而**会废掉 API route**。需要 SEO 时的正解是 Astro 或方案 C，不是 Next。
- **存储是分工不是三选一**：主文档 → KV 单键；图标 → KV 单键 + 长缓存（不占 1,000 写/天的坑：每个域名只写一次）；快照备份 → Blob（强一致、无写次数压力）；数据库 → 暂不启用。
- **三个决定性认知**：① KV 写按 key 计费（官方文档 per-key），一次 PATCH 改 100 条 = 1 次写，1,000 写/天 ≈ 每天能保存 1,000 次；② KV 真正的陷阱是最终一致 ~60s 而非额度；③ 数据库的价值在查询，而本项目数据永远是整份读整份写，上 SQL 只会白付序列化 + 往返。
- **上 D1 的触发信号**（写进方案 4.8）：点击统计/访问记录等高频写入、多用户账号、服务端查询（筛选/分页/多标签）、链接量 10,000+ 需服务端检索、需要逐条修订历史。

方案文档 `HaoNav-改造方案.md` 已同步更新：新增 4.1.1（框架实测）、4.8（存储决策），并修正第 1/3/9 节与止血步骤的数字为实测值。

### 环境备注

- 本机 npm registry 指向已废弃的 `registry.npm.taobao.org`（证书过期，install 直接失败）。**需用 `--registry=https://registry.npmmirror.com` 或 `npm_config_registry` 环境变量**。
- `/tmp` 映射到 `C:\Users\BoYan\AppData\Local\Temp`。

---

## 补充二（同日）：用户确认的四项决策

用户追问 4 个问题后，方案文档已按下列结论定稿：

1. **存储：只用 KV，Blob 与数据库都去掉。** 依据：KV 写按 key 计费，一次 PATCH 改 100 条 = 1 次写；推算实际用量约 60 次写/天（50 次保存 + 7 次快照 + 零星图标抓取），占 Cloudflare 免费 1,000 写/天 的约 6%。图标靠 `Cache-Control: immutable` 让浏览器只请求一次，省不出 KV 读取量，所以 Blob 无价值；快照每天 1 次写，Blob 的"写入无次数压力"也无意义。**引入 Blob 只会多一个要维护的绑定。**
2. **平台：国内访问为主 → EdgeOne Makers 首选；海外为主 → Cloudflare 首选；Vercel 不作目标平台。** 决定性因素不是额度（两家都够）而是中国大陆节点：EdgeOne 有大陆节点，Cloudflare/Vercel 无。因为架构已把平台差异收敛到 `api/adapters/`，**建议两家各部署一份做 A/B 实测**，增量成本几乎为零。注意 CF 的 1,000 写/天是唯一硬上限；自定义域名在国内 CDN 接入通常涉及备案，平台二级域名不需要。
3. **UI 允许重构，结构定为"左目录 + 右内容"**（与现状同构）。最快方案（已写入方案 6.1）：
   - 布局用纯 CSS Grid，零 JavaScript 计算
   - **`content-visibility: auto` + `contain-intrinsic-size` 是核心**（约 3 行 CSS 替代手写虚拟滚动，浏览器自动跳过视口外 section 的渲染/布局/绘制）
   - 预分组 `Map<catId, Link[]>` + 预建小写搜索索引（修掉当前 O(分类×链接) 重复计算）
   - 滚动联动改用 `IntersectionObserver`（当前是 scroll 事件里 getElementById + offsetTop，强制同步布局，且已被注释禁用）
   - 图标默认本地字母 SVG，零请求
   - section 级懒挂载列为**可选升级**，仅在实测滚动仍不流畅时才做（`content-visibility` 已覆盖 99% 场景）
   - 顺手增强：分类标题 sticky 吸顶、左侧分类项加计数徽章、`/` 聚焦搜索
4. **功能取舍定稿**：
   - **Chrome 扩展生成器 → 删除**。需求真实（一键存书签高频），但"站点现场生成扩展 + 手动加载已解压扩展"这个方式有四个死结（生成的代码有 bug、需手动重载或上架审核、站点更新后无自动升级、500 行模板字符串无法维护）。**替代：先做 Bookmarklet（约 1 小时），用满一个月确认高频后再做独立扩展仓库（约 1 天）。**
   - **AI 猜分类 → 删除**。分类仅 10–20 个，下拉框人眼选比等 AI 快，AI 猜错还要人工改回，净成本为负。
   - **AI 批量补描述 → 保留但必须改造**：移到服务端、改批量（一次 20–50 条，而非现在一条一请求）、仅在 `/admin` 按需触发。这是唯一真正省时间的 AI 功能。
   - 若用户不愿为 AI 付费，两个都删也成立（用批量编辑 + 从 `<title>` 自动填标题替代），且 `@google/genai` 的 75.2 KB brotli 可直接从包中消失。
   - 站外搜索简化：固定引擎 + 前缀触发，不做完整 CRUD 管理面板。
   - 新增：每日 KV 快照、重复检测、死链检测。

用户尚未下达"开始改造"的指令，仍在方案确认阶段。

---

## 补充三（同日）：最终决策锁定

用户已下达三项最终决策，方案文档 `HaoNav-改造方案.md`（717 行）已全部同步：

1. **框架确定用 Vue 3**（不再是"首选/备选"二选一）。文档 4.1 已标注"✅ 已确认"。
2. **导航逻辑确认**（写入 6.2）：与现有行为一致，用户无需重新学习操作。
   - 「全部链接」（默认）→ 置顶区 + **所有分类** section
   - 「任一具体分类」→ 置顶区（**跨分类的全部置顶链接**）+ **仅该分类** section
   - 站内搜索时只显示命中的分类，置顶区隐藏
   - **对性能设计的关键影响**：「全部链接」既是默认视图也是最重的视图（所有卡片都要渲染），`content-visibility` 主要服务于它；单分类视图天然很轻，无需优化。优化目标因此非常集中。
3. **AI 与浏览器插件全部删除**（不只是"改造"）。连带影响已核实：
   - AI：删 `services/geminiService.ts` + `@google/genai`（实测 358.3 KB raw / **49.4 KB brotli**）+ `types.ts` 的 `AIConfig`/`AIProvider` + `App.tsx` 的 `aiConfig`/`AI_CONFIG_KEY` + `LinkModal` 的 `aiConfig` prop + 原定的 `/api/ai/enrich` 接口
   - 插件：删扩展生成器（`SettingsModal` 内约 660 行模板）+ `jszip`（**唯一使用点是 `SettingsModal.tsx:729` 打包扩展文件**，实测 96.4 KB raw / **26.3 KB brotli**）
   - **合计净省 75.8 KB brotli，且不需要写任何替代代码**
   - `SettingsModal.tsx` 从 1,092 行瘦身到约 230 行（`ai` 与 `tools` 两个 tab 整体删除）
   - 新链接录入路径改为：打开本站添加 + 导入浏览器书签 HTML。**Bookmarklet 明确不在本次范围内**，仅作为未来可选项记录在文档里。
4. **R2 / Blob 确认不需要**（写入 4.8）。理由：图标靠浏览器 `Cache-Control: immutable` 只请求一次，省不出 KV 读取；快照每天 1 次写、7 份约 2.8 MB，KV 空间与写额度都远远够用；而引入它要多一个绑定、多一处凭证、多一套 SDK、多一层跨平台适配，收益为 0；R2/EdgeOne Blob 还需单独开通（CF R2 可能要求绑卡），与"只用免费套餐"冲突。
   - **真正需要对象存储的触发条件**：新增用户上传附件类功能（自定义图标原图、原始 HTML 归档、导出 zip）、数据量超 KV 的 1 GB、一天 10 万次以上图标读取。
   - **必须守住的约束**：KV 单值上限 25 MB，图标**绝不能**以 data URL 内联进主文档（会让每次保存重传数 MB），必须独立成 key。

### 当前状态

方案已定稿（11 章 + 附录），所有选型与取舍均已确认。**等用户下达开工指令。** 建议路径：先做止血 3 步（删 Tailwind CDN / 修 `Icon.tsx` 具名导入 / 删 AI 与插件），实测可把首屏 JS 从 280 KB brotli 降到约 67 KB，当天可完成、可回滚、不碰架构。

**注意：AI 与插件删除后，"动态导入 genai/jszip"这条止血建议已失效 —— 改为直接删除依赖，收益更大（75.8 KB vs 之前的动态导入方案）。**

---

## 补充四（同日）：用户选择全量改造，已建基线与团队（但等待用户复核方案）

- 用户明确指示：**不用止血方案，直接全量改造**。随后又说"现在的方案再让我看一下"，因此**暂停实施**，仅完成准备工作。
- **已完成的关键准备：建立回滚能力。** 项目原本**没有任何版本控制（无 .git）**，全量重写前必须先解决：
  - `git init` + 首次提交 → commit `f7aa488`
  - 打 tag **`v1-legacy`**（改造前原始状态，55 个文件）
  - 回滚命令：`git checkout v1-legacy -- .` 或 `git reset --hard v1-legacy`
- 已创建团队 `haonav-rewrite`，登记任务 #1（后端：共享 schema + Hono API + 存储层 + 平台适配器 + v0 迁移脚本）、#2（前台：Vue 3 导航界面 web/）。
- **尚未 spawn 任何子 agent**，尚未修改任何源码。等待用户复核方案后再开工。

### 实施时的分工约定（防止两个 agent 冲突）

- `frontend-specialist` 负责：`package.json`、`vite.config.ts`、`index.html`、`admin.html`、`tsconfig.json`、`web/`、`admin/`
- `backend-specialist` 负责：`api/`、`shared/`、`migrate/`、`tests/`，**不得修改 `package.json`**（依赖缺失只在报告里提出）
- 两人并行，靠主理人在任务 prompt 中写死的接口契约（类型 + 端点）解耦，不互相直连

---

## 补充五（同日）：修正缓存策略 —— 数据实时性设计

用户提问"后台添加链接后前端会实时显示吗，需不需要重新构建"。借此发现并修正了原方案的一个真实设计缺陷。

**修正内容（已写入文档 4.4 / 4.6 / 新增 4.9）：**

- **原设计错误**：`GET /api/data` 带 `Cache-Control: public, max-age=60, stale-while-revalidate=600`。这会让"数据延迟"变成 **CDN 缓存 60s + KV 最终一致 60s 两层叠加，最坏 1～2 分钟**，用户会直接感受到"我改了但没生效"。
- **修正为**：`/api/data` **不走 CDN 缓存**，改 `Cache-Control: no-cache` + `ETag`，`If-None-Match` 命中返回 304（响应体 0 字节）。
  - 理由：个人站读流量极小（Worker 免费 100k 请求/天绰绰有余），为省请求量而缓存的收益接近 0，但代价是伤害"编辑方便"这条硬要求。
  - 首屏速度改由 **localStorage 客户端缓存**保证（内联脚本在首帧前注入缓存 JSON），不依赖 CDN 缓存。
- **明确结论：数据在运行时从 `/api/data` 读取，不嵌入构建产物 → 改内容永远不需要重新构建，只是 1 次 KV 写。**
- **各场景生效时间**：后台自己立即（乐观更新）；刷新前台同城秒级、跨地区节点最长 60s（KV 最终一致，三家的固有特性）；**已打开的前台页面不会自己变**。
- **让已打开页面自动更新**：监听 `visibilitychange` / `focus` 时比对一次 `rev`（零轮询成本，体验最好）；如需多设备同屏同步再加 60s 轮询比对 rev（约 480 次请求/天，额度内）。**不要用 SSE** —— KV 无变更事件通知，服务端要发现变化也只能自己轮询，白占长连接。
- **这也是方案 C（构建期数据内联）默认关闭的正式理由**：它把"改一条链接"变成"等 30–90 秒重建"，用一次请求的成本换 1.5 分钟等待，不划算。
- 附带注意点：图标走 `/api/icon` 带一年 `immutable` 缓存，**替换图标时浏览器不会主动更新**，URL 需加 `?v=<hash>` 版本参数。

**教训（值得复用到其他项目）**：给"读多写少"的接口加 CDN 缓存前，先算清"写入方多久能看到自己的修改"。对个人自用型应用，缓存收益远小于"改完立刻看到"的体验价值。

---

## 补充六（同日）：方案自查 —— 发现 2 处技术性硬错误，方案重写为 v3 定稿

应要求对方案做系统性自查，重读全文后发现并修正了下列问题（文档已整体重写为 v3，881 行）。

### 🔴 技术性硬错误（2 处，必须修）

**① PBKDF2 210,000 轮在 Cloudflare Workers 免费版会直接超限 → 登录完全不可用。**
- 核实结果：**Workers Free 的 CPU 时间上限是 10 ms/请求**（Paid 为 5 min）。官方文档原文明确写着"处理认证、SSR 或解析大负载的 worker 通常要 10–20 ms"——即认证类负载本身就已踩线。
- PBKDF2-SHA256 210k 轮在边缘运行时约 100–300 ms → 必然触发 `Error 1102 Worker exceeded resource limits`。
- **修正**：改为「部署时用高熵 pepper 算一次 `ADMIN_PASSWORD_HASH` 存为 secret，登录时做 1 次 HMAC-SHA256 + 常量时间比较」，成本 < 1 ms。pepper 只存在服务端 secret，哈希即使泄漏也无法离线爆破；在线爆破由限速挡住。

**② zod 实测 74 KB brotli，方案原定"shared/schema.ts 用 zod 校验、前后端共享"会同时击穿包体预算与 CPU 预算。**
- 实测：`zod` 全量 74.0 KB brotli / `zod/v4-mini` 68.6 KB（几乎没省）。
- 放客户端：Vue 18.9 + zod 74 = **93 KB，把 45 KB 预算翻倍**。
- 放服务端：校验 2000 条链接需几十毫秒 CPU，同样超 10 ms。
- **修正**：**彻底放弃 zod**。`shared/types.ts` 只共享 TypeScript 类型（编译期，运行时 0 字节）；服务端用 `api/validate.ts` 手写轻量校验（约 40 行），深度校验只在开发环境与 Vitest 启用。

### 🆕 新增 §4.6「平台运行时的硬约束」——v2 完全遗漏的关键一节

已核实的 Workers Free 限制：**CPU 10 ms/请求**、请求 100,000/天、**子请求 50/请求**、Cron Triggers 5 个/账号、内存 128 MB、体积 3 MB。
关键洞察：**排队等网络不计 CPU**（KV 读、fetch 都不算），所以"多几次 KV 读"免费，"多做几次哈希/序列化/正则"才致命。

由此定出四条强制设计决策：
1. 密码哈希必须单次 HMAC（不能用多轮 KDF）
2. **书签导入改回客户端 Web Worker 解析 + 分批（≤200 条）提交** —— 服务端全量解析会超 CPU，而 Worker 既绕开上限又不卡 UI
3. 不用 zod 做全量文档校验
4. 死链检测、图标抓取都要分批（子请求 50/请求的上限）

**一句话原则：每次请求都要能在 10 ms 内做完。** 凡"遍历全量数据 + 复杂计算"的事，要么挪到客户端，要么拆成多批。

### 🔧 其他修正

- **结构性错位**：v2 中 4.8 的"数据库为什么用不上"与 D1 触发条件清单被误插到 4.9 末尾，已归位。
- **markdown 表格被引用块打断**：§1 的体积归因引用块插在表格中间导致渲染错乱，已移到表格后。
- **内容真实性与预算**：`changelog` 数组从文档里移除 —— 否则文档持续膨胀且每次写入重传全部历史。**改为客户端会话内保留最近 10 步快照**，撤销是纯客户端行为（撤销几乎总是"刚刚那一下"，跨设备撤销价值极低）。
- **快照调度方式补全**（v2 只写"定期自动"未写怎么触发）：优先平台 Cron（CF 免费版可用 5 个 Cron Triggers）；无 Cron 的平台退化为"当天首次写入/管理员登录时懒触发"，记 `lastSnapshotDate`。
- **`/api/icon` 补 SSRF 防护**：只允许抓已登记域名、禁私网 IP 段、限重定向、超时 3 s、失败回退字母图标。原方案完全没提，而它是个外向型代理。
- **限速实现方式明确**：按 IP 内存计数器 + 失败延迟，**不用 KV 计数**（否则烧掉 1,000/天 的写额度）。
- **`/api/export` 改为需会话**（即使是 public 读，导出也要求登录，避免被批量抓取）。
- **有序字符串澄清**：不需要完整 LexoRank 桶平衡，用 base-62 中值串即可，仅在极端窄化时做一次分类内重排。
- **`content-visibility` 补齐两个副作用**（v2 漏了）：① `contain-intrinsic-size` 估值不准会导致滚动条跳动；② **点击左侧跳转到远处分类时落点可能不准**（目标 section 未真正渲染），需跳转前临时强制渲染该 section；另有不可修复的固有限制 —— Ctrl+F 搜不到未渲染内容。
- **前后端共享成本**：`shared/types.ts` 明确"只放类型，不放运行时代码"，避免把服务端依赖拖进浏览器包体。
- **决策残留清理**：`/api/icon` 去掉 R2、平台表去掉"首选/第三备选"的矛盾表述、AI/插件删除后残留的"动态导入"建议、Bookmarklet 归属、§9 标注为"未采用"、§10 移除已跳过的 P0 止血阶段。

### ★ 新增 §4.11「站点设置模型（零硬编码）」—— 用户追加的硬性要求

用户要求"网站设置的所有东西不要写死"。体检发现写死的位置远超预期，包括 `index.html` 的 title/favicon/色板、`App.tsx` 的 `INITIAL_SITE_CONFIG` 与上游仓库地址、侧栏 logo 兜底字符 `"C"`、8 个 localStorage key 名、`types.ts` 的 `DEFAULT_CATEGORIES` 与 **`INITIAL_LINKS`（5 条写死的示例链接）**、各 Modal 的界面文案、`vite.config.ts` 的端口、API 层散落 4 个文件的 KV 绑定名与 key 名。

**三层配置模型**：
- **L1 出厂默认** → `site.config.json`（仓库内唯一配置文件，改它需重新构建）
- **L2 运行时设置** → KV 文档 `settings` 段（**改后台即秒级生效，无需重新构建**）
- **L3 部署 Secret** → 平台环境变量（KV 绑定名、会话密钥、密码哈希，不进仓库/不进 KV/不下发前端）

关键设计：**L1 与 L2 字段集合完全一致**，读取时 `{...L1, ...L2}`。用户没改过的跟随配置文件，改过的持久化在 KV。

**首屏零闪烁三段式**（"设置动态化"与"首屏不闪"的张力）：构建期把 `site.config.json` 的 title/favicon/accent 注入 `index.html` 占位符 → 解析前用内联脚本读 localStorage 缓存覆盖 → 网络返回后校正。主题色走 CSS 变量 `--accent`，所以**改主色是纯运行时行为，不需重新构建也不需改 Tailwind 配置**。

**唯一允许保留的硬编码**（诚实清单）：KV key 名与文档结构（集中 `api/keys.ts`）、接口路径、localStorage key 前缀（带版本号）、框架自身 API。
**验收方式**：`grep` 全仓搜站名字符串与硬编码 URL，命中数应为 0（`site.config.json` 与 `api/keys.ts` 除外）。

---

## 补充七（同日）：后台可配置项清单已提交用户确认（待回复）

应要求列出后台可配置项供用户圈选。我提出的版本把 §4.11 的字段做了收敛，并**主动砍掉 6 项过度设计**，等待用户确认后写回文档。

**A 组 必留（10 项）**：`title`、`navTitle`、`logo`（types: letter/emoji/image）、`favicon`、`theme.default`、`cardStyle`、`accessMode`、`searchEngines`、`footerLinks`、`description`

**B 组 建议保留（10 项）**：`lang`、`theme.accent`、`density`、`defaultCategoryView`、`openInNewTab`、`showPinnedSection`、`defaultSearchMode`、`searchPrefixes`、`iconStrategy`、`texts`（5 条文案可选覆盖）

**C 组 建议砍掉（6 项，属过度设计）**：
- `theme.radius`（圆角）—— 几乎没人改，且改它要动多处 Tailwind 类
- `stickyCategoryTitle` —— 直接默认开启，不必给开关
- `gridColumns`（暴露具体列数）—— **改为 `density: 'compact' | 'cozy' | 'spacious'` 三档预设**，理由是 Tailwind 需要静态类名，暴露裸数字实现成本高、收益低
- `snapshotRetention` / `snapshotFrequency` —— 固定"每天 1 次、保留 7 份"即可
- `footerText` —— 并入 `footerLinks`
- 左侧链接数徽章开关 —— 默认开启

**不在后台（明确边界）**：L3 部署 secret（KV 绑定名、会话签名密钥、管理员密码哈希）与内部常量（KV key 名、接口路径、localStorage key 前缀）不做成可配置项 —— 前者是安全边界，后者改它等于数据迁移。

---

## 补充八（同日）：方案整理为 v4 实施版 + 认证范围确认

### 方案 v4（应要求"去掉没用的和项目无关的介绍，检查错误遗漏，找可深化处"）

文档从 1020 行压到 **829 行**：删掉约 30% 历史论证与科普，**补齐 15 处实现遗漏**，新增两章。

**新结构**：`0 目标与结论 / 1 背景与基线（压缩） / 2 目录结构 / 3 目标架构（数据模型·设置模型·API 契约·安全设计·只用 KV·数据实时性） / 4 平台运行时硬约束 / 5 读路径与性能（含 5.3 图标策略） / 6 界面方案 / 7 后台方案 / 8 功能取舍 / 9 迁移 / 10 部署与首次配置 / 11 路线图 / 12 风险 / 附录A 决策速查 / 附录B 实现陷阱清单`

**删掉的冗余**：Next.js/Astro/Svelte 的详细论证（压成结论）、R2/D1 科普表格（压成触发条件）、§9 备选方案（压到几行）、附录"止血方案"（用户已明确不用）、旧代码 P1/P2 缺陷细节。

**补齐的 15 处遗漏**（v3 缺失，实施时都会踩）：
1. **UI 图标来源完全没写** —— 原项目用 lucide-react，新方案是 Vue，没说装什么。定为**自绘 SVG**（实测省约 5 KB）
2. 分类图标方案缺失 → 定为字母色块 + 可选 emoji，零依赖
3. 二维码库未指定 → 本地生成 + 动态 import（不进首屏）
4. **Tailwind 动态类名会被 purge**（`grid-cols-${n}`）→ 必须用完整字面量常量表
5. **快照恢复后 `rev` 必须 +1**，否则客户端误判"无变化"、界面一直显示旧数据
6. 导入的"冲突"定义 → 同 urlKey 但标题或分类不同
7. 导入的 `order` 生成 → 一律追加到目标分类末尾
8. `link.icon` 在 fetched 模式的存法 → 存相对路径 `/api/icon?u=<domain>&v=<hash>`
9. 死链检测结果不落库（避免文档膨胀）
10. 首屏加载态 → CSS 直出骨架，**不用全屏 loading 遮罩**（当前实现造成 1–3s 白屏）
11. 读失败降级 → 用缓存渲染 + 顶栏轻提示
12. 后台 rev 冲突交互 → 三选项对话框，默认"放弃并刷新"
13. **「部署与首次配置」整章缺失** → 新增第 10 章
14. 首次使用体验 → 不带种子数据，空态引导去后台导入
15. EdgeOne Cron 不确定性 → 标注"需控制台确认，否则用懒触发"

**实测新增**：20 个图标的体积对比 —— 引图标库 **5.82 KB brotli** vs 自绘 SVG path 数据 **0.52 KB**（+ 约 0.3 KB 渲染组件）。**自绘省约 5 KB**，已据此定为方案（不装任何图标库）。

### 认证范围（用户明确：不要注册登录，只要一个管理员账号密码登录后台）

文档本来就是单管理员设计，现已写显式（新增「认证范围」小节），并明确列出**没有的东西**：注册 / 邀请 / 用户管理 / 多用户 / 角色权限 / 找回密码 / 邮箱验证 / 验证码 / OAuth / 密码存储。

- 密码只以 `HMAC(password, pepper)` 存在部署 Secret 里，**本身不落任何存储**
- 登录表单只需**一个密码框**；取 30 天会话 cookie；内存计数限速
- 忘记密码**只能重设**（无"找回"）：重跑 `gen-secrets.mjs` 更新 Secret，数据不受影响
- 全站唯一登录入口 `/admin`；**前台零写操作、完全不需要登录**

### 当前状态

方案 v4 定稿（829 行 + 8 条补充记录），所有决策与遗漏均已收敛。**等用户下达开工指令。**

---

## 补充九（同日）：认证方案再简化一层（用户追问"管理员账号密码是怎么来的"）

用户问密码从哪来。借此复审了原设计，发现**原方案的 HMAC + pepper 是不必要的复杂度**，已简化为常量时间字符串比较。

### 密码来源（已确认）

**用户自己在部署时定**，配成平台环境变量。没有注册、没有初始密码、没有系统生成、没有用户表。

### 简化前后的对比

| | 原设计 | 简化后（采用） |
|---|---|---|
| 密码存放 | 本地跑脚本算出 `HMAC(密码, pepper)`，配 2 个 Secret | 直接配 `HAONAV_ADMIN_PASSWORD` |
| 登录校验 | 服务端算 HMAC 再比较 | **常量时间字符串比较** |
| 改密码 | 重跑脚本 + 更新 2 个 Secret | **改 1 个环境变量** |
| Secret 数量 | 4 项（KV + pepper + hash + session） | **3 项**（KV + 密码 + session） |

### 为什么敢去掉哈希（关键推理）

1. 平台的 Secret 写入后**无法读回明文**（Cloudflare Secrets / EdgeOne 环境变量）；
2. **更关键：会话签名密钥 `HAONAV_SESSION_SECRET` 就在同一个 Secret 存储里** —— 攻击者一旦拿到 Secret，可以直接伪造会话 cookie，**根本不需要知道密码**。所以在"Secret 已被攻破"这个场景下，密码哈希**不提供任何额外保护**；
3. 哈希唯一剩下的价值是"防密码明文意外泄漏到日志 / 错误回显"，而这靠代码纪律就能解决（已加入陷阱清单第 16 条）。

**保留 CPU 约束结论**：绝不用 PBKDF2 等多轮 KDF（10 ms CPU 上限）。常量时间比较 < 1 ms。

**可选哈希版**保留在文档里（`scripts/gen-secrets.mjs` + `HAONAV_PEPPER` / `HAONAV_PASSWORD_HASH`），但明确标注"不推荐"并说明理由。

### 新增陷阱（附录 B 增到 18 条）

- **#16 把 `env` 打日志或回显给前端** —— 这是去掉哈希后唯一真实的风险来源。当前项目就有 `details: error.message` 这类回显内部错误的写法，必须避免。
- **#17 把密码写进 `site.config.json` 或提交 `.env`** —— `site.config.json` 是要进仓库的出厂默认配置，绝不能放敏感值。
- **#18 登录失败时不区分原因** —— 统一返回"密码错误"，失败后加固定延迟。

---

## 补充十（同日）：澄清部署配置的两类（用户追问）

用户指出两处，均已修正文档。

### ① `HAONAV_KV` 不是环境变量，是「绑定」

用户说得对。我原来的表格把 KV 绑定和 Secret 混在同一张表里，误导了。

**实际是两个不同区域，不同位置：**

| | 区域 | 值的类型 | 名称 |
|---|---|---|---|
| KV 命名空间 | 控制台「**绑定 / Bindings**」 | 命名空间本身（不是字符串） | `HAONAV_KV` |
| 管理员密码 | 控制台「**环境变量 / Secrets**」 | 字符串 | `HAONAV_ADMIN_PASSWORD` |
| 会话签名密钥 | 同上 | 字符串 | `HAONAV_SESSION_SECRET` |

**准确说法：1 个绑定 + 2 个 Secret**（之前写"3 项 Secret"是错的）。文档 §10.1 已拆成两步，并加了"别找错区域"的提醒；§3.2 的 L3 行也从"部署 Secret"改为"部署配置"。

### ② `HAONAV_SESSION_SECRET` 不能随便输入

用户问是否随便输入。**不能** —— 它是会话 cookie 的**签名密钥**，一旦被猜出，攻击者可以自己伪造合法会话 cookie，**直接拿到后台权限、完全绕过密码**。必须用密码学随机数生成（文档已给 Git Bash / Linux / macOS / PowerShell 四种命令）。

两点澄清（用户容易误解的地方）：
- **不需要记住它，也不需要"输入"它** —— 它只是服务端内部签发/验签用的钥匙，生成一次粘贴一次就完事。
- **不需要备份它** —— 它不是数据。丢了或想换，重新生成一个即可，**代价只是需要重新登录一次**，数据完全不受影响。所以怀疑 cookie 泄漏时直接换掉就行。

### 实施收官（用户预览反馈第二轮完成后）

- **后端补丁**：`GET /api/backup/snapshots`（index-aside）+ `POST /api/check/links`（≤20 条、并发 5、私网段拒绝）已落地，**73 用例全绿** + typecheck 零错误（我独立核验）
- **后台左侧导航 6 分区**：链接/分类/搜索/数据/备份/设置（SearchPanel、BackupPanel 为新建，从设置与数据拆出）；admin 不 import web/ 组件（AdminIcon.vue 为复制的 path 表）
- **前台样式回归 v1-legacy**：以 `git show v1-legacy:App.tsx` 实际类名为准逐区域对齐（侧栏 w-64、渐变 logo、h-16 顶栏、胶囊切换、卡片 hover:-translate-y-0.5、Toast slideDown）；代码统一到 `web/lib/ui.ts`（GRID/LETTER_PALETTE/CARD_FRAME/SEG_ACTIVE）
- **重要构建修复**：Vite 6 中「transformIndexHtml 返回字符串 + html 内联 `<style>`」会导致 html-proxy 索引错位、`vite build` 直接失败（"No matching HTML proxy module found"）。修复：插件改 `{ order:'pre', handler }` 形式 + 内联样式迁入 app.css
- **最终实测**（我独立跑）：前台首屏 JS **35.36 KB** brotli（预算 45，改造前 280）+ CSS **5.31 KB**（预算 12，改造前 0 个 CSS 文件）；后台 admin 18.46 KB 独立入口；QR 4.05 KB 按需
- **清理遗留**：App.tsx / index.tsx / 根 types.ts / components/ / services/ / functions/ / edgefunctions/ / api/storage.ts / metadata.json / 10 张 PNG / docx / screenshots 全部删除（git 历史保留）
- **README 重写**：旧 README 描述的是旧架构部署方式（KV 绑定名与 Secret 都对不上），已按 v2 重写（含部署步骤、密钥生成、首次使用、迁移命令）
- **提交**：`5b5d7e8 feat: HaoNav v2 全量重写`（基线 `f7aa488` / tag `v1-legacy` 可回滚）
- **遗留小项**：搜索引擎的 `enabled`（启用/停用）字段未做（需在 `shared/types.ts` 的 SearchEngine 加 `enabled?: boolean`），当前已实现 CRUD + 排序
- **dev server 仍在后台运行**（task Txzo7g，5173 端口），预览可用

---

## 补充十二（同日）：实施进度与用户预览反馈

### 实施结果（全部经我独立验证，非采信成员报告）

| 阶段 | 结果 |
|---|---|
| P1 后端 | ✅ `shared/types.ts`（仅类型，grep 确认 0 处运行时代码）+ `api/` 8 文件 + 3 adapter + 迁移脚本 + 6 个测试文件；**65 用例全绿**、typecheck 零错误；core.ts 无平台符号、无 zod/PBKDF2/KV.list |
| P2 前台 | ✅ `web/` 22 文件；typecheck 零错误、build 成功；**首屏 JS 33.45 KB brotli**（预算 45） |
| P3 后台 | ✅ `admin/` 15 文件（4 面板 + 登录 + 409 冲突三选项 + 全局撤销）；typecheck 零错误；**首屏 JS 34.91 KB**、CSS 4.76 KB、后台自身 16.31 KB（不占前台预算）|
| 构建产物 | 前台 JS+CSS 合计 **39.66 KB brotli**（改造前 280 KB + 0 CSS）；QR 库 4.05 KB 按需加载 |
| 集成验证 | 前台伙伴做了 13 项真实 API 集成测试（登录→diff→PATCH→回读逐字段一致）|
| 本地预览 | dev server 5173 端口验证通过：/api/health、/api/data（3 分类 6 链接演示数据）、/admin 均 200 |

### 实施中发现并修正的问题

- 后台伙伴自行修正：**快照恢复后 rev = 当前 rev + 1**（避免客户端误判无变化）；`cat.delete` 不丢数据（保留链接置空 cat）；快照枚举 index-aside
- 前台伙伴发现的集成真 bug：**服务端空文档 `settings:{}` 必须由前端做 L1⊕L2 默认值归一化**，否则设置面板崩溃
- 前台伙伴的撤销设计优于方案：用「文档差异」现算 ops（`diffOps(lastSaved, current)`）而非收集操作，**保存后依然可撤销**
- 撤销快照只存内存（10 步），不写 localStorage/KV
- **遗留待办**：`GET /api/backup/snapshots` 与 `POST /api/check/links` 两端点缺失（已重派后端），后台已做降级容错
- 前台 JS 从 33.45 → 34.91 KB：Vue 表单 helper 被提升进双入口共享 chunk，前台多背 1.5 KB（可省但需复制 Vue 进 admin chunk，不值得）

### 用户预览反馈（第二轮修改，已派发）

1. **后台改为左侧导航，6 分区**：链接 / 分类 / **搜索**（从设置拆出）/ 数据（去备份）/ **备份**（从数据拆出：快照+导出）/ 设置。前台伙伴执行中
2. **前台样式回归原项目视觉**（以 `git show v1-legacy:App.tsx` 的实际类名为准逐区域对齐）+ **代码统一**（重复收敛到 `web/lib/ui.ts`、三档卡片渲染收进 LinkCard、行为一个不变）

### 环境备注

- `makers-install-cli` 不在 PATH，但 **edgeone CLI 已装（1.6.17 ≥ 1.6.7）且已登录**（腾讯云 1706653 / APPID 1252015513）→ 部署无需登录
- 全局 npm registry 失效（taobao 证书过期）→ 已写**项目级 `.npmrc`** 指向 npmmirror，未动用户全局配置

---

## 补充十一（同日）：正式开工 —— 团队调度与环境自检

用户下达开工指令。已按专家流程执行 Phase 2。

### 已完成的准备（主理人直接做）

| 项 | 结果 |
|---|---|
| 回滚基线 | `git init` + 首次提交 `f7aa488` + tag **`v1-legacy`**（55 个文件） |
| 团队 | `haonav-rewrite`（TeamCreate） |
| 任务登记 | #1 后端 API（backend-specialist）、#2 前台（frontend-specialist）、#3 后台（blockedBy #2）、#4 集成部署（team-lead） |
| **`edgeone` CLI** | **已存在且版本 1.6.17 ≥ 1.6.7** ✅（在托管 Node 目录 `~/.workbuddy/binaries/node/versions/22.22.2-2/edgeone`；`makers-install-cli` 脚本本身不在 PATH，但 CLI 可用） |
| **CLI 登录状态** | **已登录** ✅ 腾讯云账号 1706653 / APPID 1252015513 → **部署阶段不需要再登录** |
| 远端项目关联 | 未关联（`.edgeone/project.json` 不存在）→ 部署时用 `-n <name>` 创建新项目 |
| **npm 源问题** | 全局 registry 是失效的 `registry.npm.taobao.org`。**已写项目级 `.npmrc`**（`registry=https://registry.npmmirror.com`）—— 不改用户全局配置，但整个仓库内的 npm 操作都会自动走正确源 |

### 已调度的成员（并行）

- **`backend-specialist`** —— 交付 `shared/types.ts`（**只放类型**）、`api/{keys,store,auth,validate,urlKey,order,core}.ts`、`api/adapters/{dev,cloudflare,edgeone}.ts`、`scripts/gen-secrets.mjs`、`migrate/v0-to-v1.ts`、`tests/*`
- **`frontend-specialist`** —— 本轮只做**前台 `web/` + 构建配置**（`package.json`/`vite.config.ts`/`tsconfig.json`/`index.html`/`admin.html`/`site.config.json`/`web/styles/app.css`）。后台 `admin/` 留到其回报后由我 SendMessage 续派（这样有一个审查检查点）

### 分工边界（防止两个 agent 冲突）

- `frontend-specialist` 拥有：`package.json`、`vite.config.ts`、`tsconfig.json`、`index.html`、`admin.html`、`site.config.json`、`.npmrc` 之外的根配置、`web/**`、`admin/**`
- `backend-specialist` 拥有：`shared/**`、`api/**`、`migrate/**`、`scripts/**`、`tests/**`
- **双方都不得删除遗留文件**（`App.tsx`、`index.tsx`、根 `types.ts`、`components/`、`services/`、`functions/`、`edgefunctions/`、`api/storage.ts`），由我在集成后统一清理
- `tsconfig.json` 的 `include` 只写新目录，避免遗留 `.tsx` 因缺 react 而让 `typecheck` 失败
- 接口契约由我在两份 prompt 里写死（`shared/types.ts` 全文 + 12 个端点 + `createDevMiddleware` 这个确切导出名），两人无需互相通信

### 关键约束（已写进两份 prompt）

10 ms CPU 上限、常量时间比较密码（禁 PBKDF2）、禁用 zod、导入走客户端 Worker、`grid-cols-${n}` 会被 Tailwind purge 必须用完整字面量类名、Tailwind v4 CSS-first、前台零写操作、自绘 20 个 SVG 图标。

### 下一步

等待两位成员通过 Mailbox 回传 → 集成校验（类型一致性、首屏体积实测）→ 派后台 → dev server 预览 → 询问用户验证方式后部署。

---

## 补充十二（同日）：实施收官 —— P1–P3 全部完成并核验

### 交付与验证（我独立核验，非采信成员报告）

| 项 | 结果 |
|---|---|
| P1 后端 | ✅ `shared/types.ts`（仅类型，grep 0 处运行时代码）+ `api/` 全套 + 3 adapter + 迁移脚本；端点从 12 → **14 个**（补 `backup/snapshots`、`check/links`）；**73 用例全绿**；typecheck 零错误 |
| P2 前台 | ✅ `web/` 22 文件；typecheck/build 零错误 |
| P3 后台 | ✅ `admin/` 15→18 文件（4→6 面板）；登录 + 409 冲突三选项 + 全局撤销 |
| 构建 | ✅ typecheck 零错误、73 测试全绿（我独立跑） |
| 预览 | ✅ dev server 5173 验证：/api/health、/api/data、/admin 均 200 |

**最终体积**：前台首屏 JS **35.83 KB** brotli（预算 45，改造前 280）+ CSS **6.03 KB**（预算 12，改造前 0 个 CSS 文件）＝ 41.87 KB；后台 admin 18.72 KB 独立入口；QR 4.05 KB 按需。

### 实施中发现并修正的问题（实施级）

- 后端：快照恢复后 **rev = 当前 rev + 1**（否则客户端误判无变化）；`cat.delete` 保留链接置空 cat（不丢数据）；快照枚举 index-aside；新增 `HAONAV_CRON_SECRET` 支持 Cron 调快照
- 前端集成真 bug：**服务端空文档 `settings:{}` 必须由前端做 L1⊕L2 归一化**，否则设置面板崩溃
- 撤销设计优于方案：`diffOps(lastSaved, current)` 现算 ops，**保存后依然可撤销**；快照只存内存（10 步）
- **Tailwind 类名 purge 陷阱实际发生**：admin 面板用完整字面量替换（blue-*→emerald-*），前台动态类名全走 `web/lib/ui.ts` 常量表
- **Vite 6 构建坑**：`transformIndexHtml` 返回字符串 + html 内联 `<style>` → html-proxy 索引错位，build 失败（"No matching HTML proxy module found"）。修复：插件改 `{order:'pre', handler}` + 内联样式迁入 app.css
- 前台 JS 33.45 → 35.83 KB：Vue 表单 helper 被提升进双入口共享 chunk（多背 1.5 KB，可省但需复制 Vue 进 admin chunk，不值得）

### 用户三轮视觉/结构反馈（全部完成）

1. **后台左侧导航 6 分区**：链接/分类/**搜索**（从设置拆出）/数据（去备份）/**备份**（从数据拆出）/设置；前台伙伴新建 SearchPanel + BackupPanel
2. **前台样式回归 v1-legacy**（以 `git show v1-legacy:App.tsx` 实际类名为准）+ 代码统一到 `web/lib/ui.ts`
3. **视觉向 nav.lts.cc 对齐**（用户提供参考源码 `E:\CC\workers.js`，2652 行）：主色 blue→**emerald**（accent 默认 `#10b981`，走 `--accent` 变量保住后台改色能力）；**背景两个漂移光斑**（800px emerald + 500px teal，blur-150/120px，10s/16s 交错，prefers-reduced-motion 适配）；顶栏/侧栏**毛玻璃**；卡片半透明 + emerald 阴影；logo 渐变块 hover 缩放微旋转；**设置入口重构**为齿轮按钮 + 下拉弹层（主题三态 + 视图三选）；后台同步换肤
   - 参考站**未抄**：无侧栏布局、Tailwind CDN 交付（正是我们消灭的缺陷）、按时段自动暗色（可选未做）
   - 互相印证：参考站也用 `/api/icon?url=` 服务端图标代理 + onerror 回退，与我们 `/api/icon?u=` 设计一致

### 收尾

- 遗留文件全部清理：App.tsx / index.tsx / 根 types.ts / components/ / services/ / functions/ / edgefunctions/ / api/storage.ts / metadata.json / 10 张 PNG / docx / screenshots（git 历史保留，tag v1-legacy 可回滚）
- **README 重写**（旧 README 描述旧架构部署，是"照 README 部署必挂"的根源）：按 v2 重写，含部署步骤、密钥生成、首次使用、迁移命令
- **提交**：`f7aa488`(基线) → `5b5d7e8`(v2 重写) → `18584d6`(日志) → `43f5751`(视觉对齐)
- **dev server 仍运行**（task Txzo7g，5173），HMR 生效

### 遗留小项（不影响使用）

- 搜索引擎 `enabled`（启用/停用）字段未做（需 `shared/types.ts` SearchEngine 加 `enabled?: boolean`），当前已实现 CRUD + 排序
- 按时段自动暗色（参考站行为）未做，当前 `system` = 跟随系统

### 用户第四轮反馈（已修复并提交 2384d66）

1. **「后端有链接，但前端没有显示卡片」** —— 排查后确认数据链路与滚动链都正确，根因锁定为 `.cat-section` 的 **`content-visibility: auto`**：它依赖祖先滚动容器判定离屏，一旦判定不合会把所有分类区块判为离屏 → 整页空白。**已移除**（保留 `contain-intrinsic-size` / `scroll-margin-top`，预留 `.cat-section.is-lazy` 挂点供未来加回）。另修复 `refresh()` 增加 `updatedAt` 比较。
2. **面板归属错误**（用户原话"备份中的设置属于数据，设置中的备份才是备份分类"）→ 最终归属：**数据**=导入/导出 JSON/导出 HTML/重复检测/死链检测；**备份**=自动备份策略（开关/频率/保留份数）+ 快照（存/列表/恢复/删除）；**设置**=仅站点设置。
3. **搜索框改参考站结构** —— 引擎选择内置在搜索框左端 + 竖分隔线 + 输入框 + 清除按钮 + 右侧搜索按钮。

**顺带产出**：抽出纯函数 `web/lib/sections.ts`（预分组 + 孤儿链接归"未分类" + 跨分类置顶）提升可测试性，新增 `tests/nav-sections.test.ts`（7 例）。后端补 `DELETE /api/backup/snapshot`（含 `POST .../delete` 别名，防部分 CDN/代理丢弃 DELETE 请求体），并修了 `nav-sections.test.ts` 里 `LinkItem.createdAt` 缺失导致的 typecheck 失败。

**验证**：typecheck 零错误、**86 用例全绿**（7 文件）、build 成功；前台首屏 JS **36.10 KB** brotli、CSS **6.15 KB**（均在预算内）。

### 过程中的两个坑（值得记住）

- **沙箱内没有 Chromium**：`agent-browser install` 失败；前端成员用无头浏览器 dump 页面得到 0 字节（`.dump.html` / `.probe.txt` 全空）。**本项目无法在本机自动验证浏览器渲染**，视觉与渲染类问题只能靠用户刷新浏览器确认，或改用 Vitest + 纯函数测试验证数据链路。
- **成员可能"完成"但未提交代码**：前端第一轮回报 "completed" 却零提交（只留下调试产物）。**核验时必须看 `git log` / `git status`，不能只看完成通知。**

### 下一步

**等用户刷新浏览器确认三件事**：① 前台能看到卡片 ② 面板归属 ③ 搜索框样式 → 然后部署上线（EdgeOne CLI 已登录；控制台配 KV 绑定 `HAONAV_KV` + 2 个 Secret，步骤见 README）
